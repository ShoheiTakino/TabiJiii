<%= form_with model: [@post, @chat], class: 'form', local: true do |f| %>
  <div class="form-input">
    <%= f.text_field :comment, placeholder: 'type a message' %>
  </div>
  <%= f.submit '送信' %>
<% end %>
  <p>chatpostの表示成功</p>